@import "@/common/css/define.scss";

.des{
    margin-left: 3px
}

.name{
    flex:1;
    width: 0;

    >span{
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    >span:last-child{
        flex:1;
    }
}

.number{
    // flex: 2
}

.icon{
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: 20px;
    height: 20px;
    border-radius: 10px;
    margin-right: 8px
}